<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet"> <!-- Bootstrap Icons CSS -->
    <style>
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background: linear-gradient(to bottom right, #6a11cb 0%, #2575fc 100%);
            font-family: 'Arial', sans-serif;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px;
            background: inherit;
            color: white;
            position: relative;
        }

            .header h3 {
                margin: 0;
                font-weight: bold;
            }

        .content {
            flex: 1;
            padding: 20px;
            margin-left: 0;
        }

        .cta-button {
            background: inherit;
            color: white;
            border-radius: 30px;
            padding: 20px 50px;
            font-size: 22px;
            font-weight: 600;
            text-transform: uppercase;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

            .cta-button:hover {
                background-color: rgba(255, 255, 255, 0.3);
                transform: scale(1.05);
            }

        .profile-header {
            padding: 40px;
            border-radius: 10px;
            background-color: #ffffff;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            margin-bottom: 40px;
        }

            .profile-header img {
                width: 120px;
                height: 120px;
                border-radius: 50%;
                margin-bottom: 20px;
            }

            .profile-header h3 {
                margin-bottom: 10px;
                font-size: 1.5rem;
                font-weight: bold;
            }

            .profile-header p {
                margin-bottom: 5px;
                color: #777;
            }

        .section-header {
            margin-bottom: 25px;
            font-size: 1.8rem;
            font-weight: 600;
            color: #333;
        }

        .content-container, .career-container {
            background-color: #ffffff;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            margin-bottom: 40px;
        }

        .blog-card, .career-item {
            background-color: #ffffff;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

            .blog-card:hover, .career-item:hover {
                transform: translateY(-10px);
                box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
            }

        .blog-title {
            font-size: 1.75rem;
            font-weight: bold;
            color: #007bff;
            margin-bottom: 15px;
        }

        .blog-content {
            color: #555;
            font-size: 1rem;
            line-height: 1.5;
        }

        .status-tag {
            font-weight: 600;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.9rem;
        }

        .draft {
            background-color: #ffc107;
            color: white;
        }

        .published {
            background-color: #28a745;
            color: white;
        }

        .toast-container {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1050;
        }

        .toast-custom {
            border-radius: 10px;
            color: white;
            max-width: 350px;
        }

            .toast-custom .toast-body {
                font-weight: bold;
            }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-12 content">
                <div class="profile-header">
                    <img src="https://via.placeholder.com/120" alt="User Profile Picture">
                    <h3>@ViewBag.user.Name</h3>
                    <p>Email: @ViewBag.user.Email</p>
                </div>

                <div class="content-container">
                    <div class="section-header">Your Blogs and Drafts</div>
                    <div class="blog-card" data-id="1">
                        <h2 class="blog-title">The Future of AI in Job Market</h2>
                        <p class="blog-content">As artificial intelligence continues to evolve, its impact on the job market becomes increasingly significant...</p>
                        <span class="status-tag published">Published</span>
                        <a href="#" class="btn btn-danger" onclick="showToast(event, 'Blog deleted successfully', '/profile')">Delete</a>
                        <a href="/Home/EditBlog/1" class="btn btn-warning">Edit</a> <!-- Edit button added here -->
                    </div>

                    <div class="blog-card" data-id="2">
                        <h2 class="blog-title">How to Transition into Tech Careers</h2>
                        <p class="blog-content">Transitioning into a tech career can be a rewarding experience. Here are some steps you can take to get started...</p>
                        <span class="status-tag draft">Draft</span>
                        <a href="#" class="btn btn-success" onclick="showToast(event, 'Draft published successfully', '/blogs')">Publish</a>
                        <a href="#" class="btn btn-warning">Edit</a> <!-- Edit button added here -->
                        <a href="#" class="btn btn-danger" onclick="showToast(event, 'Draft deleted successfully', '/profile')">Delete</a>
                    </div>
                </div>

                <div class="career-container">
                    <div class="section-header">Visual Career Advisor</div>

                    <div class="content-container">
                        <h3>Career Suggestions</h3>
                        <div class="career-item">
                            <p><strong>Software Engineer</strong> - A software engineer works to develop software applications. Highly analytical and problem-solving skills are needed for this role.</p>
                            <a href="#" class="btn btn-primary">Read More</a>
                            <a href="#" class="btn btn-danger" onclick="showToast(event, 'Career suggestion deleted successfully', '/profile')">Delete</a>
                        </div>

                        <h3>Job Recommendations</h3>
                        <div class="career-item">
                            <p><strong>Full Stack Developer</strong> - Develop both the frontend and backend of applications. A combination of strong programming skills and design knowledge is crucial.</p>
                            <a href="#" class="btn btn-primary">Read More</a>
                            <a href="#" class="btn btn-danger" onclick="showToast(event, 'Job recommendation deleted successfully', '/profile')">Delete</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="toast-container">
            <div id="toast" class="toast toast-custom" role="alert" aria-live="assertive" aria-atomic="true">
                <div id="toast-message" class="toast-body">
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        function showToast(event, message, redirectUrl) {
            event.preventDefault();

            const toastElement = document.getElementById('toast');
            const toastMessage = document.getElementById('toast-message');
            toastMessage.textContent = message;

            const button = event.target;
            if (button.classList.contains('btn-danger')) {
                toastElement.classList.remove('bg-success', 'bg-warning', 'bg-primary');
                toastElement.classList.add('bg-danger');
            } else if (button.classList.contains('btn-success')) {
                toastElement.classList.remove('bg-danger', 'bg-warning', 'bg-primary');
                toastElement.classList.add('bg-success');
            } else if (button.classList.contains('btn-warning')) {
                toastElement.classList.remove('bg-success', 'bg-danger', 'bg-primary');
                toastElement.classList.add('bg-warning');
            } else if (button.classList.contains('btn-primary')) {
                toastElement.classList.remove('bg-success', 'bg-danger', 'bg-warning');
                toastElement.classList.add('bg-primary');
            }

            const toast = new bootstrap.Toast(toastElement);
            toast.show();

            setTimeout(() => {
                window.location.href = redirectUrl;
            }, 2000);
        }
    </script>
</body>
</html>
